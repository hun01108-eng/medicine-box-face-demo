# 树莓派与 Uno R3 串口通信对接说明

## 1. 串口参数

- 波特率：`9600`
- 格式：`8N1`（8位数据位、无校验、1位停止位）
- 流控：无
- 树莓派发送指令，Uno R3接收并播放对应音频

## 2. 指令格式

指令为ASCII字符串，由4位数字和换行符组成：

```text
0005\n
```

R3应按行接收，检测到`\n`后处理完整指令，并兼容忽略`\r`。

## 3. 音频编号

| 指令 | 功能 |
|---|---|
| `0004\n` | 有人经过，请吃药 |
| `0005\n` | 人脸识别成功 |
| `0006\n` | 人脸识别失败 |
| `0007\n` | 体温过高 |
| `0008\n` | 体温正常 |
| `0009\n` | 流感高风险 |
| `0010\n` | 流感中风险 |
| `0011\n` | 流感低风险 |

`0001～0003`目前树莓派不会发送，可暂时保留。

## 4. 红外播报顺序

检测到有人经过时：

```text
0004\n → 等待约2秒 → 0007\n或0008\n
```

R3需要支持按顺序播放多条指令，建议设置至少3条指令的播放队列。

## 5. 确认回复

R3接受有效指令并将其加入播放队列后，应尽快回复：

```text
ACK\n
```

树莓派等待ACK的超时时间为`2.0秒`，用于覆盖GD5800播放调用可能造成的短时阻塞。
R3不需要等待音频播放结束再回复；未知指令可回复`ERR\n`。树莓派会忽略ACK前的调试文本，
但正式联调仍建议关闭R3调试输出。未收到ACK时不会自动重发，避免重复播放。

## 6. 联调检查

- 分别发送`0004～0011`，核对播放内容。
- 检查每条有效指令是否回复`ACK\n`。
- 连续发送`0004\n`和`0007\n/0008\n`，确认依次播放。
- 连续发送3条指令，确认不会丢失、覆盖或串音。
